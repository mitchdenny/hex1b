{
  "name": "hex1b-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "generate-seo": "node scripts/generate-seo-files.mjs",
    "dev": "npm run generate-seo && vitepress dev",
    "build": "npm run generate-seo && vitepress build --outDir dist",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.5.13",
    "@xterm/xterm": "^5.5.0",
    "@xterm/addon-image": "^0.8.0",
    "@xterm/addon-unicode11": "^0.8.0",
    "@xterm/addon-web-links": "^0.11.0"
  }
}
