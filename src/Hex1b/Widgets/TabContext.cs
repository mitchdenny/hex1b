namespace Hex1b.Widgets;

/// <summary>
/// Provides a fluent API context for building tab content.
/// This context exposes widget-building methods for composing tab content.
/// </summary>
public readonly struct TabContext
{
    // TabContext is a marker type that provides access to all standard widget extension methods
    // through the WidgetContext<TabContext> pattern used in TabItemWidget content builders.
}
