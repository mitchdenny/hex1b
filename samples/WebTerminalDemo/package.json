{
  "name": "hex1b-web-terminal-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix ../../src/web-terminal run build && tsc -p tsconfig.json && node scripts/copy-assets.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hex1b/web-terminal": "file:../../src/web-terminal"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
